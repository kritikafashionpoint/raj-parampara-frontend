import React from 'react'
import CheckOutClient from './CheckOutClient'

export const metadata = {
    title: "Checkout | Raj Parampara",
    description: "Complete your order at Raj Parampara.",
};

export default function Page() {
    return (
        <div>
            <CheckOutClient />
        </div>
    )
}
